<?xml version="1.0" encoding="utf-8"?>
<!-- Row shown in the system autofill suggestion list. Text colors are set
     programmatically (OsmiumAutofillService.buildPresentation) so the row
     matches the current light/dark theme. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/autofill_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#1C1B1F"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/autofill_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#5F6368"
        android:textSize="12sp" />
</LinearLayout>
