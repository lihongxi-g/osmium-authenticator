# Osmium v2.3.0

> **Steam Guard 用户请注意：手动添加 Steam 账户时，必须在服务商（issuer）一栏填写 `Steam`，否则该账户会被当作普通 TOTP 处理，生成的验证码是错误的。**

## 更新内容

### 新增

- WebDAV 备份与恢复（设置 → 数据 → WebDAV 备份）：备份到局域网内的任意 WebDAV 服务器——NAS、电脑或另一部手机，支持主机名或局域网 IP
- 备份全程端到端加密（PBKDF2 + AES-256-GCM）：加密在设备内完成，上传的是密文，服务器上不会出现任何明文数据
- 恢复时浏览服务器上的备份列表（带时间戳）并选择其一恢复；恢复为合并模式，可勾选账户，不影响现有数据
- HOTP 账户恢复时计数器取较大值，避免从旧备份恢复后验证码失效
- 设置 → 关于 内置用户协议与隐私政策
- 使用手册新增 WebDAV 备份章节，9 种语言同步更新
- 新增单元测试：WebDAV 客户端端到端测试（模拟服务器）、备份加解密往返、导入合并逻辑

### 修改

- 应用新增 INTERNET 权限，仅用于用户自行配置的 WebDAV 备份服务器；应用不会连接任何其他地址，无遥测、无云
- 隐私政策与用户协议同步更新对网络权限的说明（设置 → 关于 内可查看）

## 下载

| 文件 | 架构 |
|---|---|
| app-arm64-v8a-release.apk | arm64-v8a（推荐，现代手机） |
| app-armeabi-v7a-release.apk | armeabi-v7a（老款 32 位手机） |
| app-x86_64-release.apk | x86_64（模拟器） |

安装与设备架构不符的包会导致启动闪退。
