# Osmium 隐私政策

*生效日期：2026 年 9 月 4 日*

Osmium 是一款隐私优先的 Android TOTP/HOTP 验证器。本政策以通俗语言说明本应用对您的数据做什么、不做什么。

## 我们收集的数据

**我们不收集、不出售、不用于广告分析任何个人数据。**具体说明：

- 没有账号体系、没有注册、没有统计分析、没有广告 SDK、没有崩溃上报。
- 我们不运行 Osmium 云端账户或同步服务器。
- 应用的网络请求仅发生在您启用/执行相应功能时：用户配置的 WebDAV 备份、GitHub 更新检查，以及您主动开始的局域网快捷传输。

## 存储在本机的数据

- 您的验证器密钥、账户名、服务商、标签及标签颜色仅存储在您的设备上，使用 Android Keystore 中不可导出的密钥进行 AES‑256‑GCM 加密。
- 您导出的备份文件经过密码加密（PBKDF2 + AES‑256‑GCM），保存在您自行选择的位置；应用开发者无法访问。
- 可选的应用 PIN 与自毁 PIN 以加盐哈希或受保护的密文形式存储，无法由开发者恢复。
- WebDAV 服务器地址与登录信息存储在本机，密码经 Android Keystore 保护。
- 标签功能开关和标签筛选状态属于应用设置/界面状态；关闭标签功能不会把标签上传到任何服务，也不会删除已经保存的标签数据。

## 网络：WebDAV 备份

如果您启用 WebDAV 备份，应用只连接您填写的服务器地址，且只在您测试连接、上传备份、获取备份列表或恢复备份时连接。备份文件在离开设备前已用您的导出密码加密，服务器上只存放密文。以 `http://` 开头的明文地址需要您在屏幕上明确确认后才会保存。服务器地址可以是局域网设备，也可以是您自行管理的远程服务器；请自行确认其可信性。

## 网络：GitHub 更新检查

如果自动检查更新处于开启状态（默认开启），应用会向 GitHub Releases API（`api.github.com`）查询公开版本信息。请求不包含账户数据或设备标识。检查失败时静默跳过，应用不会自行下载或安装更新。关闭开关后，正常设置加载完成后不会主动执行更新检查；冷启动期间，在保存的设置完成加载前可能存在一次检查触发窗口。

## 网络：局域网快捷传输

局域网快捷传输由您主动在两台设备之间启动。两台设备需要连接同一 Wi‑Fi；应用通过局域网发现或您输入的 IP/端口建立临时连接，并使用 6 位配对码派生的 AES‑256‑GCM 加密传输账户数据。账户数据不经过 Osmium 云端或其他第三方服务器。接收方在导入前可以预览并选择账户。局域网环境、路由器隔离和其他设备的行为不由 Osmium 控制，因此请只向预期接收方提供配对码。

## 自动备份与后台运行

如果您启用自动备份，应用通过 Android 系统调度器按计划执行，可选择 WebDAV 或手机的 下载/Osmium 目录。备份密码存储在本机并由 Android Keystore 保护，仅用于加密备份文件。本地备份按您设置的保留数量清理。应用不驻留常驻后台服务，但厂商省电策略可能推迟或拦截定时任务。

自动备份页面可以请求系统弹出电池优化豁免对话框；是否授权由 Android 系统决定，应用不会从中读取额外个人数据。

## 权限说明

- **相机**——仅用于导入账户时扫描二维码；画面在设备本地处理，不存储、不传输。
- **生物识别 / 设备凭据**——完全由 Android 系统处理，应用不接触指纹或人脸数据。
- **网络**——用于上述用户配置的 WebDAV、GitHub 更新检查和局域网快捷传输。
- **Wi‑Fi 状态与组播**——用于局域网设备发现和连接；局域网传输页面退出后会停止发现/服务。
- **存储（仅 Android 8/9）**——`WRITE_EXTERNAL_STORAGE` 仅用于将自动备份写入公共的 下载/Osmium 目录；Android 10 及更高版本无需此权限。
- **ACCESS_LOCAL_NETWORK**——为未来 Android/targetSdk 对局域网访问的权限要求预先声明；当前 `targetSdk 34` 不主动请求该运行时权限。

## 数据共享

我们不向广告商、分析服务或 Osmium 云端共享数据，因为我们不持有这些数据。WebDAV 服务器会收到您主动上传的加密备份文件；局域网中的预期接收设备会收到您主动发起传输的账户数据。GitHub 只收到公开版本查询请求，不收到账户或设备数据。

## 数据删除

- 卸载应用或清除应用数据会删除应用本地数据，但不会自动删除您已经导出的文件、公共下载目录中的备份、WebDAV 服务器上的备份或另一台设备上已经接收的数据。
- 配置并输入自毁 PIN 后，应用会按设计不可逆销毁本机账户、PIN、设置和本地加密主密钥；它不会替您删除其他位置的备份或接收副本。
- 您可以自行删除导出文件和 WebDAV/局域网接收副本；自动备份还会按保留数量清理旧文件。

## 政策变更

本应用为 GPL‑3.0-or-later 许可证开源软件。若未来改变上述数据处理方式，将在发布说明与本文件中明确告知。

## 联系方式

邮箱：zhif0776@hotmail.com · Telegram：https://t.me/osmium2fa
